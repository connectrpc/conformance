// Copyright 2023 The Connect Authors
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: connectrpc/conformance/v2/service.proto

package conformancev2connect

import (
	v2 "connectrpc.com/conformance/internal/gen/proto/go/connectrpc/conformance/v2"
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion0_1_0

const (
	// ConformanceServiceName is the fully-qualified name of the ConformanceService service.
	ConformanceServiceName = "connectrpc.conformance.v2.ConformanceService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ConformanceServiceUnaryProcedure is the fully-qualified name of the ConformanceService's Unary
	// RPC.
	ConformanceServiceUnaryProcedure = "/connectrpc.conformance.v2.ConformanceService/Unary"
	// ConformanceServiceServerStreamProcedure is the fully-qualified name of the ConformanceService's
	// ServerStream RPC.
	ConformanceServiceServerStreamProcedure = "/connectrpc.conformance.v2.ConformanceService/ServerStream"
	// ConformanceServiceClientStreamProcedure is the fully-qualified name of the ConformanceService's
	// ClientStream RPC.
	ConformanceServiceClientStreamProcedure = "/connectrpc.conformance.v2.ConformanceService/ClientStream"
	// ConformanceServiceBidiStreamProcedure is the fully-qualified name of the ConformanceService's
	// BidiStream RPC.
	ConformanceServiceBidiStreamProcedure = "/connectrpc.conformance.v2.ConformanceService/BidiStream"
	// ConformanceServiceUnimplementedProcedure is the fully-qualified name of the ConformanceService's
	// Unimplemented RPC.
	ConformanceServiceUnimplementedProcedure = "/connectrpc.conformance.v2.ConformanceService/Unimplemented"
)

// ConformanceServiceClient is a client for the connectrpc.conformance.v2.ConformanceService
// service.
type ConformanceServiceClient interface {
	// A unary operation. The request indicates the response headers and trailers
	// and also indicates either a response message or an error to send back.
	//
	// Response message data is specified as bytes. The service should echo back
	// request properties in the ConformancePayload and then include the message
	// data in the data field.
	//
	// Servers should allow the response definition to be unset in the request and
	// if it is, set no response headers or trailers and send back an empty response.
	Unary(context.Context, *connect.Request[v2.UnaryRequest]) (*connect.Response[v2.UnaryResponse], error)
	// A server-streaming operation. The request indicates the response headers,
	// response messages, trailers, and an optional error to send back. The
	// response data should be sent in the order indicated, and the server should
	// wait between sending response messages as indicated.
	//
	// Response message data is specified as bytes. The service should echo back
	// request properties in the first ConformancePayload, and then include the
	// message data in the data field. Subsequent messages after the first one
	// should contain only the data field.
	//
	// Servers should allow the response definition to be unset in the request and
	// if so, all responses should contain no response headers or trailers and
	// contain empty response data.
	ServerStream(context.Context, *connect.Request[v2.ServerStreamRequest]) (*connect.ServerStreamForClient[v2.ServerStreamResponse], error)
	// Servers should allow the response definition to be unset in the request and
	// if it is, set no response headers or trailers and send back empty response data.
	ClientStream(context.Context) *connect.ClientStreamForClient[v2.ClientStreamRequest, v2.ClientStreamResponse]
	// A bidirectional-streaming operation. The first request indicates the response
	// headers, response messages, trailers, and an optional error to send back.
	// The response data should be sent in the order indicated, and the server
	// should wait between sending response messages as indicated. If the
	// full_duplex field is true, the handler should read one request
	// and then send back one response, and then alternate, reading another
	// request and then sending back another response, etc. If the response_delay_ms
	// duration is specified, the server should wait that long in between sending each
	// response message. If both are specified, the server should wait the given
	// duration after reading the request before sending the corresponding
	// response.
	//
	// Response message data is specified as bytes. The service should echo back
	// request properties, including all request messages *so far*, in the order
	// they were received, in the ConformancePayload and then include the message
	// data in the data field. The request headers and timeout only need to be
	// included in the very first response message.
	//
	// If the input stream is empty, the server should send a single response
	// message that includes no data and only the request properties (headers,
	// timeout).
	BidiStream(context.Context) *connect.BidiStreamForClient[v2.BidiStreamRequest, v2.BidiStreamResponse]
	// A unary endpoint that the server should not implement and should instead
	// return an unimplemented error when invoked.
	Unimplemented(context.Context, *connect.Request[v2.UnimplementedRequest]) (*connect.Response[v2.UnimplementedResponse], error)
}

// NewConformanceServiceClient constructs a client for the
// connectrpc.conformance.v2.ConformanceService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewConformanceServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ConformanceServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &conformanceServiceClient{
		unary: connect.NewClient[v2.UnaryRequest, v2.UnaryResponse](
			httpClient,
			baseURL+ConformanceServiceUnaryProcedure,
			opts...,
		),
		serverStream: connect.NewClient[v2.ServerStreamRequest, v2.ServerStreamResponse](
			httpClient,
			baseURL+ConformanceServiceServerStreamProcedure,
			opts...,
		),
		clientStream: connect.NewClient[v2.ClientStreamRequest, v2.ClientStreamResponse](
			httpClient,
			baseURL+ConformanceServiceClientStreamProcedure,
			opts...,
		),
		bidiStream: connect.NewClient[v2.BidiStreamRequest, v2.BidiStreamResponse](
			httpClient,
			baseURL+ConformanceServiceBidiStreamProcedure,
			opts...,
		),
		unimplemented: connect.NewClient[v2.UnimplementedRequest, v2.UnimplementedResponse](
			httpClient,
			baseURL+ConformanceServiceUnimplementedProcedure,
			opts...,
		),
	}
}

// conformanceServiceClient implements ConformanceServiceClient.
type conformanceServiceClient struct {
	unary         *connect.Client[v2.UnaryRequest, v2.UnaryResponse]
	serverStream  *connect.Client[v2.ServerStreamRequest, v2.ServerStreamResponse]
	clientStream  *connect.Client[v2.ClientStreamRequest, v2.ClientStreamResponse]
	bidiStream    *connect.Client[v2.BidiStreamRequest, v2.BidiStreamResponse]
	unimplemented *connect.Client[v2.UnimplementedRequest, v2.UnimplementedResponse]
}

// Unary calls connectrpc.conformance.v2.ConformanceService.Unary.
func (c *conformanceServiceClient) Unary(ctx context.Context, req *connect.Request[v2.UnaryRequest]) (*connect.Response[v2.UnaryResponse], error) {
	return c.unary.CallUnary(ctx, req)
}

// ServerStream calls connectrpc.conformance.v2.ConformanceService.ServerStream.
func (c *conformanceServiceClient) ServerStream(ctx context.Context, req *connect.Request[v2.ServerStreamRequest]) (*connect.ServerStreamForClient[v2.ServerStreamResponse], error) {
	return c.serverStream.CallServerStream(ctx, req)
}

// ClientStream calls connectrpc.conformance.v2.ConformanceService.ClientStream.
func (c *conformanceServiceClient) ClientStream(ctx context.Context) *connect.ClientStreamForClient[v2.ClientStreamRequest, v2.ClientStreamResponse] {
	return c.clientStream.CallClientStream(ctx)
}

// BidiStream calls connectrpc.conformance.v2.ConformanceService.BidiStream.
func (c *conformanceServiceClient) BidiStream(ctx context.Context) *connect.BidiStreamForClient[v2.BidiStreamRequest, v2.BidiStreamResponse] {
	return c.bidiStream.CallBidiStream(ctx)
}

// Unimplemented calls connectrpc.conformance.v2.ConformanceService.Unimplemented.
func (c *conformanceServiceClient) Unimplemented(ctx context.Context, req *connect.Request[v2.UnimplementedRequest]) (*connect.Response[v2.UnimplementedResponse], error) {
	return c.unimplemented.CallUnary(ctx, req)
}

// ConformanceServiceHandler is an implementation of the
// connectrpc.conformance.v2.ConformanceService service.
type ConformanceServiceHandler interface {
	// A unary operation. The request indicates the response headers and trailers
	// and also indicates either a response message or an error to send back.
	//
	// Response message data is specified as bytes. The service should echo back
	// request properties in the ConformancePayload and then include the message
	// data in the data field.
	//
	// Servers should allow the response definition to be unset in the request and
	// if it is, set no response headers or trailers and send back an empty response.
	Unary(context.Context, *connect.Request[v2.UnaryRequest]) (*connect.Response[v2.UnaryResponse], error)
	// A server-streaming operation. The request indicates the response headers,
	// response messages, trailers, and an optional error to send back. The
	// response data should be sent in the order indicated, and the server should
	// wait between sending response messages as indicated.
	//
	// Response message data is specified as bytes. The service should echo back
	// request properties in the first ConformancePayload, and then include the
	// message data in the data field. Subsequent messages after the first one
	// should contain only the data field.
	//
	// Servers should allow the response definition to be unset in the request and
	// if so, all responses should contain no response headers or trailers and
	// contain empty response data.
	ServerStream(context.Context, *connect.Request[v2.ServerStreamRequest], *connect.ServerStream[v2.ServerStreamResponse]) error
	// Servers should allow the response definition to be unset in the request and
	// if it is, set no response headers or trailers and send back empty response data.
	ClientStream(context.Context, *connect.ClientStream[v2.ClientStreamRequest]) (*connect.Response[v2.ClientStreamResponse], error)
	// A bidirectional-streaming operation. The first request indicates the response
	// headers, response messages, trailers, and an optional error to send back.
	// The response data should be sent in the order indicated, and the server
	// should wait between sending response messages as indicated. If the
	// full_duplex field is true, the handler should read one request
	// and then send back one response, and then alternate, reading another
	// request and then sending back another response, etc. If the response_delay_ms
	// duration is specified, the server should wait that long in between sending each
	// response message. If both are specified, the server should wait the given
	// duration after reading the request before sending the corresponding
	// response.
	//
	// Response message data is specified as bytes. The service should echo back
	// request properties, including all request messages *so far*, in the order
	// they were received, in the ConformancePayload and then include the message
	// data in the data field. The request headers and timeout only need to be
	// included in the very first response message.
	//
	// If the input stream is empty, the server should send a single response
	// message that includes no data and only the request properties (headers,
	// timeout).
	BidiStream(context.Context, *connect.BidiStream[v2.BidiStreamRequest, v2.BidiStreamResponse]) error
	// A unary endpoint that the server should not implement and should instead
	// return an unimplemented error when invoked.
	Unimplemented(context.Context, *connect.Request[v2.UnimplementedRequest]) (*connect.Response[v2.UnimplementedResponse], error)
}

// NewConformanceServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewConformanceServiceHandler(svc ConformanceServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	conformanceServiceUnaryHandler := connect.NewUnaryHandler(
		ConformanceServiceUnaryProcedure,
		svc.Unary,
		opts...,
	)
	conformanceServiceServerStreamHandler := connect.NewServerStreamHandler(
		ConformanceServiceServerStreamProcedure,
		svc.ServerStream,
		opts...,
	)
	conformanceServiceClientStreamHandler := connect.NewClientStreamHandler(
		ConformanceServiceClientStreamProcedure,
		svc.ClientStream,
		opts...,
	)
	conformanceServiceBidiStreamHandler := connect.NewBidiStreamHandler(
		ConformanceServiceBidiStreamProcedure,
		svc.BidiStream,
		opts...,
	)
	conformanceServiceUnimplementedHandler := connect.NewUnaryHandler(
		ConformanceServiceUnimplementedProcedure,
		svc.Unimplemented,
		opts...,
	)
	return "/connectrpc.conformance.v2.ConformanceService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ConformanceServiceUnaryProcedure:
			conformanceServiceUnaryHandler.ServeHTTP(w, r)
		case ConformanceServiceServerStreamProcedure:
			conformanceServiceServerStreamHandler.ServeHTTP(w, r)
		case ConformanceServiceClientStreamProcedure:
			conformanceServiceClientStreamHandler.ServeHTTP(w, r)
		case ConformanceServiceBidiStreamProcedure:
			conformanceServiceBidiStreamHandler.ServeHTTP(w, r)
		case ConformanceServiceUnimplementedProcedure:
			conformanceServiceUnimplementedHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedConformanceServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedConformanceServiceHandler struct{}

func (UnimplementedConformanceServiceHandler) Unary(context.Context, *connect.Request[v2.UnaryRequest]) (*connect.Response[v2.UnaryResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("connectrpc.conformance.v2.ConformanceService.Unary is not implemented"))
}

func (UnimplementedConformanceServiceHandler) ServerStream(context.Context, *connect.Request[v2.ServerStreamRequest], *connect.ServerStream[v2.ServerStreamResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("connectrpc.conformance.v2.ConformanceService.ServerStream is not implemented"))
}

func (UnimplementedConformanceServiceHandler) ClientStream(context.Context, *connect.ClientStream[v2.ClientStreamRequest]) (*connect.Response[v2.ClientStreamResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("connectrpc.conformance.v2.ConformanceService.ClientStream is not implemented"))
}

func (UnimplementedConformanceServiceHandler) BidiStream(context.Context, *connect.BidiStream[v2.BidiStreamRequest, v2.BidiStreamResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("connectrpc.conformance.v2.ConformanceService.BidiStream is not implemented"))
}

func (UnimplementedConformanceServiceHandler) Unimplemented(context.Context, *connect.Request[v2.UnimplementedRequest]) (*connect.Response[v2.UnimplementedResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("connectrpc.conformance.v2.ConformanceService.Unimplemented is not implemented"))
}
